import { Button } from "@mui/material";

function StartButton() {
    return (
      <div>
        <Button variant="contained" href="/numtype">スタート</Button>
      </div>
    );
  }
  
  export default StartButton;